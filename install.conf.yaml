- defaults:
    link:
        relink: true
        create: true

- clean: ['~']

- link:
    ~/.config/plasma-workspace/env/set_window_manager.sh:
    ~/.config/fish/config.fish:
    ~/.config/fish/functions:
    ~/.ghci:
    ~/.gitconfig:
    ~/.stack/config.yaml:
    ~/.vimrc:
    ~/.xmonad/xmonad.hs:
